# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4
AZURE_OPENAI_API_VERSION=2024-08-01-preview

# Optional: Use API Key (for local dev) - Remove for production with Managed Identity
# AZURE_OPENAI_API_KEY=your-api-key-here

# Server Configuration
PORT=3000

# Mongo (Azure Cosmos DB - Mongo API)
# Example (replace <user>,<password>,<database>):
# MONGO_URI="mongodb://<user>:<password>@openarmsai.global.mongocluster.cosmos.azure.com:10255/<database>?ssl=true&replicaSet=globaldb&retrywrites=false"

# Debug
QNA_DEBUG=false
